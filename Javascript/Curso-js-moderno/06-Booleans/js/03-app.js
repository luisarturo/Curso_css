const autenticado = true;

if ( autenticado === true ) {
    console.log( "Si puedes ver netflix");
} else {
    console.log("No puedes verlo")
}

// Operador ternario
console.log(autenticado ? "Si puede ver netflix " : "No puedes verlo.");